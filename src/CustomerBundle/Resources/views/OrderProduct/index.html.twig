{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderProduct list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Order_id</th>
                <th>Product_id</th>
                <th>Quantity</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('orderproduct_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.orderid }}</td>
                <td>{{ entity.productid }}</td>
                <td>{{ entity.quantity }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('orderproduct_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orderproduct_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('orderproduct_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
