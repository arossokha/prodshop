{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OrderProduct</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Order_id</th>
                <td>{{ entity.orderid }}</td>
            </tr>
            <tr>
                <th>Product_id</th>
                <td>{{ entity.productid }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ entity.quantity }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('orderproduct') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('orderproduct_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
